FOLDERS = env.FOLDERS
DESTINATION = env.DESTINATION
SOURCE = env.SOURCE

node('rsync') {
    for(String folder : FOLDERS.split(",")) {
        stage("Synch ${folder}") {
           sh "rsync -av ${SOURCE}:${folder} ${DESTINATION}"
        }
    }
}